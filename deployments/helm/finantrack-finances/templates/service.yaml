apiVersion: v1
kind: Service
metadata:
  name: {{ include "finantrack-finances.fullname" . }}
  labels:
    {{- include "finantrack-finances.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "finantrack-finances.selectorLabels" . | nindent 4 }}
