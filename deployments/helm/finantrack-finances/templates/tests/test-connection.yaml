apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "finantrack-finances.fullname" . }}-test-connection"
  labels:
    {{- include "finantrack-finances.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "finantrack-finances.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
